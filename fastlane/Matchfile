git_url("git@github.com:mapbox/apple-certificates.git")
type("appstore") # The default type, can be: appstore, adhoc, enterprise or development
app_identifier(["com.mapbox.examples"])
username(ENV['MAPBOX_APPLE_ID_EMAIL'])